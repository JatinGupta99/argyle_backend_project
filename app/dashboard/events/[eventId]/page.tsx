'use client';

import { FeatureComingSoon } from '@/components/shared/FeatureComingSoon';

export default function EventPage() {
    return (
        <FeatureComingSoon
            title="Event Dashboard"
            description="Welcome to the event command center. We're currently building a high-performance dashboard to help you manage and experience this event like never before."
        />
    );
}
